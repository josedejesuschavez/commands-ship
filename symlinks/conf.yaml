- clean: ['~']

- defaults:
    link:
        create: true
        force: true
      
- link:
    ~/.gitconfig: $DOTFILES_PATH/git/.gitconfig
    ~/.zimrc: $DOTFILES_PATH/shell/zsh/.zimrc
    ~/.zlogin: $DOTFILES_PATH/shell/zsh/.zlogin
    ~/.zshenv: $DOTFILES_PATH/shell/zsh/.zshenv
    ~/.zshrc: $DOTFILES_PATH/shell/zsh/.zshrc
